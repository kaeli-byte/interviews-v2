{
  "buildCommand": "cd frontend && npm install && npm run build && cp -r out/* .. && cd .. && pip install -r requirements.txt",
  "framework": "fastapi",
  "installCommand": "pip install -r requirements.txt"
}